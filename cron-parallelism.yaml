apiVersion: batch/v1
kind: Job
metadata:
  name: helloworld
spec:
  completions: 4
  parallelism: 1
  template:
    spec:
      containers:
      - name: nginx
        image: nginx
        command: ["sleep", "90"]
      restartPolicy: Never
