apiVersion: batch/v1
kind: Job
metadata:
  name: helloworld
spec:
  template:
    spec:
      containers:
      - name: nginx
        image: nginx
        command: ["echo", "Helloo Kube!!!"]
      restartPolicy: Never
