apiVersion: v1
kind: Pod
metadata:
  name: sec-capability
  namespace: default
spec:
  containers:
  - image: nginx
    name: alpine
    securityContext:
      capabilities:
        drop:
          - NET_RAW
          - CHOWN
